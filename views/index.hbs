{{!-- {{log this}} --}}

<header>
    {{#if session.currentUser}}
    <div>
        <a href="/reading-list">📑Reading list</a>
        <a href="/auth/user-profile">⚙️User Profile</a>
    </div>

    <span><strong>Your Feeds Reader</strong></span>

    <a href="/auth/logout">Logout from {{_locals.session.currentUser.email}}</a>
    {{else}}
        <div>
            <a href="/auth/login">Login</a>
            <a href="/auth/signup">Sign up</a>
        </div>
    {{/if}}
</header>

    {{#if session.currentUser}}
        {{> feeds this}}
    {{else}}
        Please <a href="/auth/login">Login</a> to see your feeds.
        Don't have an account, please <a href="/auth/signup">Sign up</a>.
    {{/if}}
