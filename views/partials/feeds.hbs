
{{log "feedContent" this}}


{{#each this}}

    <h2>{{title}} ({{items.length}})</h2>
        <ul>
        {{#each items}}
            <li>
                <span>{{formatedDate}}</span>
                <span><a href="{{id}}" target="_blank">{{title}}</a></span>
            </li>
        {{/each}}
        </ul>
{{/each}}