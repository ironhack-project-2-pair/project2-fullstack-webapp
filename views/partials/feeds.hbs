
{{!-- {{log "feedContent" this}} --}}

{{#each this.feeds}}
    <h2>{{title}} ({{items.length}})</h2>
        <ul>
        {{#each items}}
            <li>
                <span>[{{formatedDate}} | <a onclick="(() => addToReadList('{{title}}','{{id}}'))()" 
                    href="javascript:void(0);" >📑</a>]
                </span>
                <span><a href="{{link}}" target="_blank">{{title}}</a></span>
            </li>
        {{/each}}
        </ul>
{{else}}
    <p>All feeds with error 😥</p>
{{/each}}

{{#if this.feedsInError}}
    <div>
        <h3>Feeds with error</h3>
        <ul>
        {{#each this.feedsInError}}
            <li>
                {{url}}
                <br/>
                {{errorMessage}}
            </li>
        {{/each}}
        </ul>
        
    </div>
{{/if}}